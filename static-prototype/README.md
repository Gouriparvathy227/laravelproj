# Static Prototype Snapshot

This folder preserves assets from the original static prototype.

- `assets/` contains the pre-Laravel CSS/JS files that were moved from the project root.
- The active Laravel application uses `public/assets/` and Blade templates in `resources/views/`.
